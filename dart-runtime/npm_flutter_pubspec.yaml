# THIS FILE MUST NOT BE COMMITED AS PUBSPEC.YAML

name: fishyjoes_dart
version: 0.0.1
homepage:

environment:
  sdk: '>=3.11.5 <4.0.0'
  flutter: '>=3.41.7'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  ffi: ^2.0.1
  tuple: ^2.0.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  lints: ^2.0.0
  yaml: ^3.1.2

flutter:
  plugin:
    platforms:
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
