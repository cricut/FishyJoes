# Python Runtime Non-Goals

This initial package intentionally does not solve:

- PyPy compatibility
- free-threaded CPython / no-GIL support
- subinterpreter isolation
- packaging policy for every platform at once
- automatic code generation for every FishyJoes type without generator support
- direct Swift source parsing inside Python

