# __AUTOGEN_WARNING__

[project]
name = "__PYTHON_DISTRIBUTION_NAME__"
version = "__PYTHON_PACKAGE_VERSION__"
description = "Python bindings generated by FishyJoes for __MODULE_NAME__"
requires-python = ">=3.11"
dependencies = [__PYTHON_DEPENDENCIES__
]

[tool.uv.sources]__PYTHON_DEPENDENCY_SOURCES__

[build-system]
requires = ["hatchling>=1.31.0,<2"]
build-backend = "hatchling.build"

[tool.mypy]
strict = true

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build_hook.py"

[tool.hatch.metadata]
allow-direct-references = true

[pytest]
testpaths = ["../tests"]
