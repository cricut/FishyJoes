[project]
name = "__MODULE_NAME__-tests"
version = "0.1.0"
description = "Python tests for __MODULE_NAME__"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9,<10",
    "testapi",
]

[tool.uv.sources]
testapi = { path = "../generated", editable = true }
