cffi
coverage
hatchling
# The typing gates (stubtest/mypy) are contract-sensitive to the checker
# version: stubtest's member coverage and error set shift across releases,
# which silently changes which allowlist entries are needed. Pin, and move
# deliberately.
mypy==2.1.0
packaging
pyright==1.1.411
types-cffi
auditwheel; sys_platform == "linux"
delocate; sys_platform == "darwin"
