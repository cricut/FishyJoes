# THIS FILE MUST NOT BE COMMITED AS PUBSPEC.YAML

name: cricut___LOWERCASE_MODULE_NAME__
description: "A Flutter wrapper for __MODULE_NAME__"
version: 0.0.1

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  fishyjoes_dart:
    path: ../flutter_fishyjoesruntime
  freezed_annotation: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
