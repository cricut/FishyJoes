name: cricut___LOWERCASE_MODULE_NAME__
description: "A Flutter wrapper for __MODULE_NAME__"
version: "0.0.1-unknown"

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: '>=3.3.0'

dependencies:__PUBSPEC_FLUTTER_DEPENDENCIES__

  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
