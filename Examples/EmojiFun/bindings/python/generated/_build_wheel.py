# THIS FILE IS AUTOMATICALLY GENERATED, AND WILL BE OVERWRITTEN. DO NOT EDIT.

from __future__ import annotations

import argparse
import base64
import hashlib
import json
import platform
import re
import shutil
import subprocess
import sys
import sysconfig
import tempfile
import tomllib
import zipfile
from pathlib import Path


ROOT = Path(__file__).resolve().parent
PYPROJECT = tomllib.loads((ROOT / "pyproject.toml").read_text(encoding="utf-8"))
WHEEL_METADATA_NAME = "__fishyjoes_wheel.json"
NATIVE_DEPENDENCIES = []


def normalized_distribution_name(name: str) -> str:
    return re.sub(r"[-_.]+", "_", name).lower()


def normalized_platform(value: str) -> str:
    return value.replace("-", "_").replace(".", "_")


def native_library_paths() -> list[Path]:
    native_dir = ROOT / "native"
    if not native_dir.exists():
        return []
    suffixes = {".dylib", ".so", ".dll"}
    return [path for path in sorted(native_dir.rglob("*")) if path.suffix.lower() in suffixes]


def native_library_filename(name: str) -> str:
    system = platform.system()
    if system == "Darwin":
        return f"lib{name}.dylib"
    if system == "Linux":
        return f"lib{name}.so"
    if system == "Windows":
        return f"{name}.dll"
    raise RuntimeError(f"Unsupported Python binding platform: {system}")


def dependency_native_library_filenames() -> set[str]:
    names = {native_library_filename("FishyJoesIotaRuntime")}
    if not NATIVE_DEPENDENCIES:
        return names
    for dependency in NATIVE_DEPENDENCIES:
        module = dependency["module"]
        names.add(native_library_filename(module))
        names.add(native_library_filename(f"{module}-iota"))
    return names


def lipo_architectures(path: Path) -> set[str]:
    if shutil.which("lipo") is None:
        return set()
    result = subprocess.run(
        ["lipo", "-archs", str(path)],
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.DEVNULL,
        check=False,
    )
    if result.returncode != 0:
        return set()
    return set(result.stdout.split())


def wheel_architectures() -> list[str]:
    if platform.system() == "Darwin":
        native_paths = native_library_paths()
        arch_sets = [lipo_architectures(path) for path in native_paths]
        if arch_sets and all({"arm64", "x86_64"}.issubset(arch_set) for arch_set in arch_sets):
            return ["arm64", "x86_64"]
    machine = platform.machine() or sysconfig.get_platform().split("-")[-1]
    return [normalized_platform(machine)]


def wheel_platform_tag() -> str:
    tag = normalized_platform(sysconfig.get_platform())
    if platform.system() == "Darwin" and set(wheel_architectures()) == {"arm64", "x86_64"}:
        parts = tag.split("_")
        if parts[-1] in {"arm64", "x86_64", "universal2"}:
            parts[-1] = "universal2"
            return "_".join(parts)
    return tag


def wheel_metadata(platform_tag: str) -> dict[str, object]:
    return {
        "generator": "fishy-joes",
        "python_tag": "py3",
        "abi_tag": "none",
        "platform_tag": platform_tag,
        "python_requirement": PYPROJECT["project"]["requires-python"],
        "platform_system": platform.system(),
        "architectures": wheel_architectures(),
    }


def wheel_hash(data: bytes) -> str:
    digest = hashlib.sha256(data).digest()
    return "sha256=" + base64.urlsafe_b64encode(digest).rstrip(b"=").decode("ascii")


def package_root() -> tuple[str, Path]:
    packages = PYPROJECT["tool"]["hatch"]["build"]["targets"]["wheel"]["packages"]
    package_path = Path(packages[0])
    return package_path.name, ROOT / package_path


def iter_package_files(package_name: str, source_dir: Path):
    dependency_native_filenames = dependency_native_library_filenames()
    for path in sorted(source_dir.rglob("*")):
        if path.is_file() and "__pycache__" not in path.parts:
            yield Path(package_name) / path.relative_to(source_dir), path
    native_dir = ROOT / "native"
    if native_dir.exists():
        for path in sorted(native_dir.rglob("*")):
            if path.is_file() and path.name not in dependency_native_filenames:
                yield Path(package_name) / "native" / path.relative_to(native_dir), path


def shared_runtime_library_reference() -> str | None:
    system = platform.system()
    if system == "Darwin":
        return "@loader_path/../../fishyjoes_runtime/native/libFishyJoesIotaRuntime.dylib"
    return None


def wheel_has_shared_runtime_reference(wheel_path: Path) -> bool:
    shared_runtime = shared_runtime_library_reference()
    if shared_runtime is None:
        return False
    with zipfile.ZipFile(wheel_path) as archive:
        return any(shared_runtime.encode("utf-8") in archive.read(name) for name in archive.namelist())


def patched_native_file_bytes(source_path: Path) -> bytes:
    shared_runtime = shared_runtime_library_reference()
    if shared_runtime is None or platform.system() != "Darwin" or source_path.suffix != ".dylib":
        return source_path.read_bytes()
    install_name_tool = shutil.which("install_name_tool")
    if install_name_tool is None:
        raise RuntimeError("install_name_tool is required to package macOS Python binding wheels")
    with tempfile.TemporaryDirectory() as temp:
        patched = Path(temp) / source_path.name
        shutil.copy2(source_path, patched)
        patched.chmod(patched.stat().st_mode | 0o200)
        for current_name in [
            "@loader_path/libFishyJoesIotaRuntime.dylib",
            "@rpath/libFishyJoesIotaRuntime.dylib",
        ]:
            subprocess.run(
                [install_name_tool, "-change", current_name, shared_runtime, str(patched)],
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                check=False,
            )
        return patched.read_bytes()


def build_wheel(outdir: Path, version_override: str | None = None) -> Path:
    project = PYPROJECT["project"]
    distribution = normalized_distribution_name(project["name"])
    version = version_override or project["version"]
    package_name, source_dir = package_root()
    platform_tag = wheel_platform_tag()
    tag = f"py3-none-{platform_tag}"
    dist_info = f"{distribution}-{version}.dist-info"
    wheel_path = outdir / f"{distribution}-{version}-{tag}.whl"

    outdir.mkdir(parents=True, exist_ok=True)
    records: list[tuple[str, str, str]] = []

    def write_bytes(archive: zipfile.ZipFile, name: str, data: bytes) -> None:
        archive.writestr(name, data)
        records.append((name, wheel_hash(data), str(len(data))))

    with zipfile.ZipFile(wheel_path, "w", compression=zipfile.ZIP_DEFLATED) as archive:
        for archive_path, source_path in iter_package_files(package_name, source_dir):
            data = patched_native_file_bytes(source_path)
            if version_override is not None and archive_path == Path(package_name) / "_native.py":
                data = data.replace(
                    f'package_version="{project["version"]}"'.encode("utf-8"),
                    f'package_version="{version_override}"'.encode("utf-8"),
                )
            write_bytes(archive, archive_path.as_posix(), data)
        write_bytes(
            archive,
            (Path(package_name) / WHEEL_METADATA_NAME).as_posix(),
            json.dumps(wheel_metadata(platform_tag), indent=2, sort_keys=True).encode("utf-8"),
        )

        metadata = [
            "Metadata-Version: 2.3",
            f"Name: {project['name']}",
            f"Version: {version}",
            f"Requires-Python: {project['requires-python']}",
        ]
        if description := project.get("description"):
            metadata.append(f"Summary: {description}")
        for dependency in project.get("dependencies", []):
            metadata.append(f"Requires-Dist: {dependency}")
        metadata.append("")
        write_bytes(archive, f"{dist_info}/METADATA", "\n".join(metadata).encode("utf-8"))

        wheel = [
            "Wheel-Version: 1.0",
            "Generator: fishy-joes",
            "Root-Is-Purelib: false",
            f"Tag: {tag}",
            "",
        ]
        write_bytes(archive, f"{dist_info}/WHEEL", "\n".join(wheel).encode("utf-8"))

        record_name = f"{dist_info}/RECORD"
        record_lines = [",".join(record) for record in records]
        record_lines.append(f"{record_name},,")
        archive.writestr(record_name, "\n".join(record_lines).encode("utf-8"))

    return wheel_path


def run_command(command: list[str]) -> subprocess.CompletedProcess[str]:
    return subprocess.run(
        command,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        check=False,
    )


def tool_path(name: str) -> str | None:
    for executable in [
        Path(sys.executable).parent / name,
        Path(sys.executable).resolve().parent / name,
    ]:
        if executable.exists():
            return str(executable)
    return shutil.which(name)


def replace_with_repaired_wheel(original: Path, repaired_dir: Path) -> Path:
    repaired_wheels = sorted(repaired_dir.glob("*.whl"))
    if len(repaired_wheels) != 1:
        raise RuntimeError(f"Expected one repaired wheel, found {[path.name for path in repaired_wheels]}")
    repaired = repaired_wheels[0]
    destination = original.parent / repaired.name
    if destination != original and original.exists():
        original.unlink()
    shutil.move(str(repaired), destination)
    return destination


def write_repair_report(outdir: Path, platform_name: str, lines: list[str]) -> None:
    report = outdir / f"native-repair-report-{platform_name}.txt"
    report.write_text("\n".join(lines) + "\n", encoding="utf-8")


def repair_wheel(wheel_path: Path) -> Path:
    system = platform.system()
    platform_name = normalized_platform(system.lower())
    report_lines = [
        f"wheel={wheel_path.name}",
        f"platform={wheel_platform_tag()}",
        f"system={system}",
        f"architectures={','.join(wheel_architectures())}",
        f"dependency_native_libraries={json.dumps(sorted(dependency_native_library_filenames()))}",
    ]
    if system == "Darwin":
        delocate_wheel = tool_path("delocate-wheel")
        if delocate_wheel is None:
            raise RuntimeError("delocate-wheel is required to repair macOS Python binding wheels")
        with tempfile.TemporaryDirectory() as temp:
            command = [delocate_wheel, "--verbose", "-w", temp]
            if NATIVE_DEPENDENCIES or wheel_has_shared_runtime_reference(wheel_path):
                command.append("--ignore-missing-dependencies")
            command.append(str(wheel_path))
            result = run_command(command)
            report_lines.append(result.stdout.rstrip())
            if result.returncode != 0:
                raise RuntimeError(f"delocate-wheel failed for {wheel_path.name}:\n{result.stdout}")
            repaired = replace_with_repaired_wheel(wheel_path, Path(temp))
        report_lines.append(f"repaired={repaired.name}")
        write_repair_report(wheel_path.parent, platform_name, report_lines)
        return repaired
    if system == "Linux":
        auditwheel = tool_path("auditwheel")
        if auditwheel is None:
            raise RuntimeError("auditwheel is required to validate Linux Python binding wheels")
        show = run_command([auditwheel, "show", str(wheel_path)])
        report_lines.append(show.stdout.rstrip())
        with tempfile.TemporaryDirectory() as temp:
            repair = run_command([auditwheel, "repair", "-w", temp, str(wheel_path)])
            report_lines.append(repair.stdout.rstrip())
            if repair.returncode == 0:
                repaired = replace_with_repaired_wheel(wheel_path, Path(temp))
                report_lines.append(f"repaired={repaired.name}")
                write_repair_report(wheel_path.parent, platform_name, report_lines)
                return repaired
        report_lines.append("repair=not-manylinux-compatible; kept honest platform-specific Linux wheel tag for non-PyPI or private-index distribution")
        write_repair_report(wheel_path.parent, platform_name, report_lines)
        return wheel_path
    if system == "Windows":
        dlls = [str(path.relative_to(ROOT)) for path in native_library_paths() if path.suffix.lower() == ".dll"]
        report_lines.append(f"dlls={json.dumps(dlls, sort_keys=True)}")
        report_lines.append("repair=package-local DLL loading is validated by runtime import tests")
        write_repair_report(wheel_path.parent, platform_name, report_lines)
        return wheel_path
    raise RuntimeError(f"Unsupported Python binding platform: {system}")


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--outdir", type=Path, default=ROOT / "dist")
    parser.add_argument("--no-repair", action="store_true", help="Build the wheel without native-library repair")
    parser.add_argument("--version-override", help="Build a wheel with this package version without editing generated files")
    args = parser.parse_args()
    wheel = build_wheel(args.outdir, version_override=args.version_override)
    if not args.no_repair:
        wheel = repair_wheel(wheel)
    print(wheel)


if __name__ == "__main__":
    main()
