// THIS FILE IS AUTOMATICALLY GENERATED, AND WILL BE OVERWRITTEN. DO NOT EDIT.
import { defineConfig } from 'vitest/config'

// Required so SharedArrayBuffer is available — the worker-threads runtime depends on it.
const crossOriginIsolationHeaders = {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
}

export default defineConfig({
  resolve: {
    alias: {
      'EmojiFun': '@cricut/emojifun-wasm',
    },
  },
  // Don't prebundle the wasm package — we need Vite to honor its `browser` field
  // (EmojiFun.browser.js) and serve EmojiFun.wasm / EmojiFun.worker.js as static assets.
  optimizeDeps: {
    exclude: ['@cricut/emojifun-wasm'],
  },
  server: {
    headers: crossOriginIsolationHeaders,
    fs: {
      // The @cricut/emojifun-wasm dependency is installed via a `file:` link to
      // ../generated/packages/wasm/, which resolves outside the test project root.
      // Allow Vite to serve the wasm + worker assets from there.
      allow: ['..', '../..'],
    },
  },
  preview: {
    headers: crossOriginIsolationHeaders,
  },
  test: {
    globals: true,
    include: ['*.test.ts'],
    setupFiles: ['generated/vitest.setup.wasm-browser.ts'],
    browser: {
      enabled: true,
      provider: 'playwright',
      name: 'chromium',
      headless: true,
      // Don't leave behind __screenshots__/ PNGs when tests fail.
      screenshotFailures: false,
      api: {
        headers: crossOriginIsolationHeaders,
      },
    },
  },
})
