name: cricut_test_api
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^2.17.1

dependencies:
  path: ^1.8.0
  dart_runtime:
    path: ../../../dart-runtime

dev_dependencies:
  ffigen: ^7.2.0
  lints: ^2.0.0
  test: ^1.16.0
