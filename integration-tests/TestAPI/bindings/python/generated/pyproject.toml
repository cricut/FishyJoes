# THIS FILE IS AUTOMATICALLY GENERATED, AND WILL BE OVERWRITTEN. DO NOT EDIT.

[project]
name = "testapi"
version = "0.0.1"
description = "Python bindings generated by FishyJoes for TestAPI"
requires-python = ">=3.11"
dependencies = [
  "fishyjoes-runtime~=0.0",

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
# Development builds against a local-path dependency express it as a PEP 508
# direct reference (name @ file:///…); allow the build backend to accept it.
# Published wheels never contain direct references (pack rejects filesystem,
# branch, and revision dependencies without a concrete version requirement).
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/testapi"]
artifacts = ["native/*"]
