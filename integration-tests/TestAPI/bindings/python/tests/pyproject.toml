[project]
name = "TestAPI-tests"
version = "0.1.0"
description = "Tests for TestAPI"
requires-python = ">=3.11"
dependencies = [
    "pyrefly>=1.1.1",
    "pytest>=9.1.1",
    "testapi",
]

[tool.uv.sources]
testapi = { path = "../generated", editable = true }
