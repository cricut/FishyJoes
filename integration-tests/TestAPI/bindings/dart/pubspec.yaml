name: cricut_testapi
description: "A Dart wrapper for TestAPI"
version: 0.0.1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  fishyjoes_dart:
    # path: ../../FishyJoes/dart-runtime
    git:
      url: https://github.com/cricut/FishyJoes.git
      ref: "(replace this string with latest fishyjoes version)"
      path: dart-runtime

  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  build_runner: ^2.2.0
